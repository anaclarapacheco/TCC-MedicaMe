"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
const testing_1 = require("@angular-devkit/schematics/testing");
const schematics_1 = require("@angular-devkit/schematics");
const test_1 = require("@schematics/angular/utility/test");
const test_utils_1 = require("../test-utils");
const utils_1 = require("../utils");
const ts_utils_1 = require("../ts-utils");
describe('Migrate component schematic', () => {
    const project = 'some-project';
    const componentName = 'a';
    const moduleName = 'b';
    const componentClassName = 'AComponent';
    const schematicRunner = new testing_1.SchematicTestRunner('@nativescript/schematics', path_1.join(__dirname, '../collection.json'));
    let appTree;
    beforeEach(() => __awaiter(void 0, void 0, void 0, function* () {
        appTree = new testing_1.UnitTestTree(new schematics_1.HostTree());
        appTree = yield setupProject(appTree, schematicRunner, project);
    }));
    describe('When the name of existing component is provided', () => {
        const options = {
            project,
            name: componentName,
            style: true,
        };
        const htmlComponentPath = `/src/app/${componentName}/${componentName}.component.html`;
        const xmlComponentPath = `/src/app/${componentName}/${componentName}.component.tns.html`;
        beforeEach(() => __awaiter(void 0, void 0, void 0, function* () {
            appTree = yield schematicRunner.runSchematicAsync('component', {
                name: componentName,
                nativescript: false,
                web: true,
                project,
            }, appTree)
                .toPromise();
            appTree = yield schematicRunner.runSchematicAsync('migrate-component', options, appTree).toPromise();
        }));
        it('should create an {N} markup file for the component', () => {
            expect(appTree.files).toContain(xmlComponentPath);
        });
        it('should declare the component in the correct NgModule', () => {
            const nsModulePath = `/src/app/app.module.tns.ts`;
            const content = test_1.getFileContent(appTree, nsModulePath);
            const matcher = test_utils_1.isInModuleMetadata('AppModule', 'declarations', componentClassName, true);
            expect(content).toMatch(matcher);
        });
        it('should import the component in the correct NgModule using @src', () => {
            const nsModulePath = `/src/app/app.module.tns.ts`;
            const source = ts_utils_1.getSourceFile(appTree, nsModulePath);
            const imports = ts_utils_1.findImports(componentClassName, source);
            expect(imports.length).toEqual(1);
            expect(imports[0].getFullText()).toContain(`@src/app/${componentName}/${componentName}.component`);
        });
        it('should put the original web template in the {N} markup file', () => {
            const html = test_1.getFileContent(appTree, htmlComponentPath);
            const xml = test_1.getFileContent(appTree, xmlComponentPath);
            expect(xml.includes(html)).toBeTruthy();
        });
    });
    describe('When component imported in another module is provided', () => {
        const options = {
            project,
            name: componentName,
            module: moduleName,
            style: true,
        };
        const htmlComponentPath = `/src/app/${componentName}/${componentName}.component.html`;
        const xmlComponentPath = `/src/app/${componentName}/${componentName}.component.tns.html`;
        beforeEach(() => __awaiter(void 0, void 0, void 0, function* () {
            appTree = yield schematicRunner.runSchematicAsync('module', {
                project,
                name: moduleName,
            }, appTree)
                .toPromise();
            appTree = yield schematicRunner.runSchematicAsync('component', {
                project,
                nativescript: false,
                web: true,
                name: componentName,
                module: moduleName,
            }, appTree)
                .toPromise();
            appTree = yield schematicRunner.runSchematicAsync('migrate-component', Object.assign({}, options), appTree).toPromise();
        }));
        it('should create an {N} markup file for the component', () => {
            expect(appTree.files).toContain(xmlComponentPath);
        });
        it('should declare the component in the correct NgModule', () => {
            const nsModulePath = `/src/app/${moduleName}/${moduleName}.module.tns.ts`;
            const content = test_1.getFileContent(appTree, nsModulePath);
            const matcher = test_utils_1.isInModuleMetadata('BModule', 'declarations', componentClassName, true);
            expect(content).toMatch(matcher);
        });
        it('should import the component in the correct NgModule using @src', () => {
            const nsModulePath = `/src/app/${moduleName}/${moduleName}.module.tns.ts`;
            const source = ts_utils_1.getSourceFile(appTree, nsModulePath);
            const imports = ts_utils_1.findImports(componentClassName, source);
            expect(imports.length).toEqual(1);
            expect(imports[0].getFullText()).toContain(`@src/app/${componentName}/${componentName}.component`);
        });
        it('should put the original web template in the {N} markup file', () => {
            const html = test_1.getFileContent(appTree, htmlComponentPath);
            const xml = test_1.getFileContent(appTree, xmlComponentPath);
            expect(xml.includes(html)).toBeTruthy();
        });
    });
});
const setupProject = (appTree, schematicRunner, project) => __awaiter(void 0, void 0, void 0, function* () {
    appTree = yield schematicRunner.runSchematicAsync('shared', {
        name: project,
        prefix: '',
        sourceDir: 'src',
        style: 'css',
        theme: true,
        sample: false,
    }, appTree).toPromise();
    appTree = utils_1.moveToRoot(schematicRunner, appTree, project);
    return appTree;
});
//# sourceMappingURL=index_spec.js.map